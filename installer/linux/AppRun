#!/bin/sh
# AppImage entrypoint for Say No to Bloatware.
# Resolves its own location and launches the bundled self-contained app.
HERE="$(dirname "$(readlink -f "$0")")"
export PATH="$HERE/usr/bin:$PATH"
exec "$HERE/usr/bin/SNB.Desktop" "$@"
